name: e2e-ci

on:
  pull_request:
    paths:
      - "apps/api/**"
      - "apps/web/**"
      - "packages/openapi/**"
      - ".github/workflows/e2e-ci.yml"

jobs:
  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: E2E placeholder
        run: echo "Playwright smoke tests will be added after Sprint 0 app shell is wired."
